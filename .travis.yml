sudo: required
services:
  - docker

before_install:
  - docker build -t cksiew/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true cksiew/react-test npm test -- --coverage

after_success:
  - docker build -t cksiew/multi-client ./client
  - docker build -t cksiew/multi-nginx ./nginx
  - docker build -t cksiew/multi-server ./server
  - docker build -t cksiew/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push cksiew/multi-client
  - docker push cksiew/multi-nginx
  - docker push cksiew/multi-server
  - docker push cksiew/multi-worker